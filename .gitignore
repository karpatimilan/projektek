# Firebase / Google services config — contains project API keys, keep out of git
google-services.json
GoogleService-Info.plist

# macOS / editor junk
.DS_Store
*.lck

# KiCad autosave history (nested git repos, not source)
.history/
